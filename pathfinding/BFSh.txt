#pragma once

#ifndef BFS_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define BFS_H


int** bfs(int startlocR, int startlocC, int targetlocR, int targetlocC);


#endif